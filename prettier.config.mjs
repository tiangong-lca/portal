/** @type {import("prettier").Config} */
const config = {
  plugins: ["prettier-plugin-tailwindcss"],
  printWidth: 100,
  proseWrap: "never",
  singleQuote: false,
  trailingComma: "all",
};

export default config;
