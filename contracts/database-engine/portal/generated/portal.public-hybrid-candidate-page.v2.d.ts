// Source: ../portal.public-hybrid-candidate-page.v2.schema.json
// Generator: json-schema-to-typescript@15.0.4
/* eslint-disable */
/**
 * This file was automatically generated by json-schema-to-typescript.
 * DO NOT MODIFY IT BY HAND. Instead, modify the source JSONSchema file,
 * and run json-schema-to-typescript to regenerate this file.
 */

/**
 * Strict locator-free version-aware Hybrid page. Both branches retain up to 200 eligible exact versions. Datasets are ranked by their best matching version before pagination; every recalled version is retained in its bounded version group. This is not exhaustive corpus recall.
 */
export type TianGongPortalPublicHybridCandidatePageV2 = {
  [k: string]: unknown;
} & {
  schemaVersion: "portal.public-hybrid-candidate-page.v2";
  kind: "process" | "flow";
  /**
   * Lowercase SHA-256 binding normalized kind/terms, embedding digest, filters, limit and the isolated portal-hybrid-rank-v2 namespace.
   */
  queryFingerprint: string;
  /**
   * @maxItems 20
   */
  items: Candidate[];
  /**
   * Number of exact public versions in this bounded candidate union, at most 400; not a global result count.
   */
  candidateCount: number;
  /**
   * Opaque keyset continuation for the same query, filters, embedding and page size. Null when this candidate union has no remaining page.
   */
  nextCursor: string | null;
  /**
   * Distinct dataset IDs in the bounded candidate union. Page limits count these groups, not individual versions.
   */
  datasetCount: number;
  /**
   * One group per representative item, in the same order. Matches are sorted by exact-version score, then version descending; the first key equals the representative key. No candidate version in a returned group is suppressed.
   *
   * @maxItems 20
   */
  versionGroups: VersionGroup[];
};
export type LocalizedText = LocalizedTextItem[];
/**
 * This interface was referenced by `undefined`'s JSON-Schema
 * via the `definition` "HybridMatch".
 */
export type HybridMatch = {
  [k: string]: unknown;
} & {
  kind: "hybrid";
  /**
   * Each recall branch is bounded to 200 eligible exact versions. Deterministic 0.5/0.5 RRF (k=60), normalized by 61, is fused only on id/version. Underfilled ANN remains underfilled; no fill-driven full-corpus exact scan. Ranked candidates may differ from v1 and do not imply globally exhaustive recall. Groups are ordered by their best exact-version score before pagination, never by the sum of version scores.
   */
  algorithmVersion: "portal-hybrid-rank-v2";
  /**
   * Finite normalized ranking explanation, never a domain quantity.
   */
  score: number;
  /**
   * @minItems 1
   * @maxItems 2
   */
  reasonCodes: [
    "lexical_public_projection" | "semantic_public_projection",
    ...("lexical_public_projection" | "semantic_public_projection")[]
  ];
  evidence: HybridEvidence;
};
/**
 * This interface was referenced by `undefined`'s JSON-Schema
 * via the `definition` "HybridEvidence".
 */
export type HybridEvidence = {
  [k: string]: unknown;
} & {
  lexicalRank: number | null;
  semanticRank: number | null;
  semanticDistance: NonNegativeCanonicalDecimal | null;
};
/**
 * This interface was referenced by `undefined`'s JSON-Schema
 * via the `definition` "NonNegativeCanonicalDecimal".
 */
export type NonNegativeCanonicalDecimal = string;

/**
 * This interface was referenced by `undefined`'s JSON-Schema
 * via the `definition` "Candidate".
 */
export interface Candidate {
  key: PublicDatasetKey;
  accessLevel: "open" | "metadata_only";
  capabilities: PublicCapabilities;
  names: LocalizedText;
  summary: LocalizedText;
  geography: Geography;
  referenceYear: number | null;
  context: PublicCardContext;
  modifiedAt: string;
  match: HybridMatch;
}
/**
 * Exact public data-set identity. An ID is never accepted without its version.
 */
export interface PublicDatasetKey {
  kind: "process" | "flow";
  /**
   * TIDAS UUID: a lowercase 16-byte hexadecimal identifier.
   */
  id: string;
  /**
   * Exact database data-set version in the fixed NN.NN.NNN form.
   */
  version: string;
}
export interface PublicCapabilities {
  metadataVisible: true;
  exchangesVisible: boolean;
  lciaVisible: boolean;
  publicArtifactVisible: boolean;
  citationVisible: true;
  policyVersion: string;
  reasonCodes: string[];
}
export interface LocalizedTextItem {
  language: string;
  value: string;
}
export interface Geography {
  code: string | null;
  label: LocalizedText;
  precision: "country" | "province" | "city" | "other" | "unknown";
}
/**
 * Shared, exhaustive, locator-free context emitted byte-identically by lexical Search and Hybrid candidate cards before their versioned match objects are added.
 */
export interface PublicCardContext {
  reference: PublicCardReference;
  functionalUnit: CompleteFunctionalUnit | null;
  technology: LocalizedText;
  source: PublicSource;
  quality: PublicCardQuality;
}
/**
 * Evidence-backed display reference for a Search or Hybrid card. Missing public evidence is represented by an empty name array, never by an authored locator or private fallback.
 */
export interface PublicCardReference {
  kind: "reference_product" | "reference_flow_property";
  name: LocalizedText;
}
/**
 * Functional-unit context required before a public numeric value may be emitted.
 */
export interface CompleteFunctionalUnit {
  /**
   * Canonical non-exponent decimal string derived from TIDAS Real. Producers must remove a leading plus sign, leading integer zeroes, trailing fractional zeroes, and negative zero before emitting it. The value contains 1 to 38 total digits and consumers must not coerce it to a binary floating-point number.
   */
  amount: string;
  unit: string;
  description: LocalizedText;
}
export interface PublicSource {
  databaseId: string | null;
  databaseVersion: string | null;
  sourceRecordId: string | null;
  providerName: LocalizedText;
  licenseId: string | null;
  licenseUrl: string | null;
}
/**
 * Public quality status copied only from the exhaustive public data-set metadata allowlist. It is unrelated to private workflow Review rows.
 */
export interface PublicCardQuality {
  reviewStatus: string | null;
}
/**
 * This interface was referenced by `undefined`'s JSON-Schema
 * via the `definition` "VersionGroup".
 */
export interface VersionGroup {
  key: PublicDatasetKey;
  /**
   * @minItems 1
   * @maxItems 400
   */
  matches: [VersionMatch, ...VersionMatch[]];
}
/**
 * This interface was referenced by `undefined`'s JSON-Schema
 * via the `definition` "VersionMatch".
 */
export interface VersionMatch {
  key: PublicDatasetKey;
  match: HybridMatch;
}
